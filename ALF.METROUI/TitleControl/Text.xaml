<UserControl x:Class="ALF.METROUI.TitleControl.Text"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="mainGrid" Height="50">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock  x:Name="titleText" Margin="5" VerticalAlignment="Center" HorizontalAlignment="Right"  />
        <Border  Grid.Column="1"  Grid.RowSpan="2" Margin="5" >
            <TextBox   x:Name="valueText" Background="Transparent" VerticalContentAlignment="Center"  />
        </Border>
    </Grid>
</UserControl>
